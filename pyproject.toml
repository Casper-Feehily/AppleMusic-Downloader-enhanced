[project]
name = "applemusic-dl"
description = "一个功能强大的 Apple Music 下载工具，支持下载歌曲、音乐视频。"
requires-python = ">=3.10"
authors = [{ name = "wenfeng110402" }]
dependencies = [
    "gamdl~=3.8.5",
    "click>=8.0",
    "colorama",
    "pillow",
    "fastapi>=0.100",
    "uvicorn>=0.23",
    "pydantic>=2.0",
    "httpx-retries>=0.6.0",
    "certifi",
]
readme = "README.md"
dynamic = ["version"]

[project.optional-dependencies]
desktop = [
    "pywebview",
]

[project.urls]
homepage = "https://github.com/wenfeng110402/AppleMusic-Downloader"
repository = "https://github.com/wenfeng110402/AppleMusic-Downloader"

[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"

[project.scripts]
amdl = "amdl.cli:main"

[tool.flit.module]
name = "amdl"
